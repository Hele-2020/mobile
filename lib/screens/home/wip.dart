import 'package:flutter/material.dart';

class WipScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text("En construction")),
        body: Text("Cette page est en construction"));
  }
}
